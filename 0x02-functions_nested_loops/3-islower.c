#include "main.h"

/**
 * _islower - check if lower case
 * @c: char param
 *
 * Description: returns one for lowercase
 *
 * Return: 0 for uppercase, 1 for lowercase
 */
int _islower(int c)

{
	if (c >= 'a' && c <= 'z')
	{
		retrun (1);
	}

	else
	{
		return (0);
	}

}
